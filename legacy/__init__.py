"""Ṛta — preserved legacy material (see legacy/LEGACY_README.md).

Nothing here is on the launch path. These artifacts are kept for historical
reference and provenance; the hard rule is that nothing is deleted, only
moved into legacy/.
"""
