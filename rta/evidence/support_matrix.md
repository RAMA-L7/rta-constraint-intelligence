# SDC Command Support Matrix (Phase 7)

How completely the validator analyzes each SDC command. Generated by
`benchmarks/generate_support_matrix.py` from `support_boundary.py`.

| Command | Level | Interpreted options | Standard options silently ignored |
|---|---|---|---|
| `create_clock` | PARTIAL | -name, -period, -waveform | -add, -comment |
| `create_generated_clock` | PARTIAL | -divide_by, -master_clock, -multiply_by, -name, -period, -source | -add, -combinational, -duty_cycle, -edge_shift, -invert, -preinvert |
| `create_voltage_area` | PARTIAL | — | -guard_band_x, -guard_band_y, -name, -region |
| `group_path` | PARTIAL | — | -from, -name, -through, -to, -weight |
| `set_case_analysis` | FULL | — | — |
| `set_clock_gating_check` | PARTIAL | — | -clock, -fall, -high, -hold, -low, -rise, -setup |
| `set_clock_groups` | PARTIAL | -asynchronous, -group, -logically_exclusive, -physically_exclusive | -allow_async_paths, -allow_paths |
| `set_clock_jitter` | PARTIAL | — | -clock, -early, -fall, -hold, -late, -rise, -setup, -source |
| `set_clock_latency` | PARTIAL | — | -clock, -early, -fall, -late, -max, -min, -rise, -source |
| `set_clock_sense` | UNSUPPORTED | — | -clock, -negative, -positive, -pulse, -stop_propagation |
| `set_clock_transition` | PARTIAL | — | -clock, -fall, -max, -min, -rise |
| `set_clock_uncertainty` | PARTIAL | -fall, -hold, -rise, -setup | -clock, -from, -source, -to |
| `set_data_check` | PARTIAL | -clock | -clock_gating_check, -fall_from, -fall_to, -from, -hold, -rise_from, -rise_to, -setup, -to |
| `set_disable_timing` | PARTIAL | -from, -to | -through, -type |
| `set_dont_touch` | FULL | — | — |
| `set_dont_use` | FULL | — | — |
| `set_drive` | PARTIAL | — | -clock, -fall, -max, -min, -rise |
| `set_driving_cell` | PARTIAL | — | -clock, -dont_scale, -fall, -from_pin, -input_transition_fall, -input_transition_rise, -lib_cell, -library, -max, -min, -pin, -rise, -to_pin |
| `set_false_path` | PARTIAL | -from, -through, -to | -fall_from, -fall_to, -hold, -rise_from, -rise_to, -setup |
| `set_ideal_latency` | UNSUPPORTED | — | -clock, -early, -fall, -late, -max, -min, -rise, -source |
| `set_ideal_network` | PARTIAL | — | -clock, -delay, -no_propagate |
| `set_ideal_transition` | UNSUPPORTED | — | -clock, -fall, -max, -min, -rise |
| `set_input_delay` | PARTIAL | -clock, -max, -min | -add_delay, -clock_fall, -disable_clock_gating_check, -fall, -level_sensitive, -network_latency_included, -reference_pin, -rise, -source_latency_included |
| `set_input_transition` | PARTIAL | — | -clock, -fall, -max, -min, -rise |
| `set_load` | PARTIAL | — | -clock, -fall, -max, -min, -pin_load, -rise, -wire_load |
| `set_max_area` | FULL | — | — |
| `set_max_capacitance` | PARTIAL | — | -clock, -fall, -max, -min, -rise |
| `set_max_delay` | PARTIAL | -datapath_only | -fall_from, -fall_to, -from, -hold, -ignore_clock_latency, -rise_from, -rise_to, -setup, -through, -to |
| `set_max_dynamic_power` | FULL | — | — |
| `set_max_fanout` | PARTIAL | — | -clock |
| `set_max_leakage_power` | FULL | — | — |
| `set_max_transition` | PARTIAL | — | -clock, -fall, -max, -min, -rise |
| `set_min_capacitance` | PARTIAL | — | -clock, -fall, -max, -min, -rise |
| `set_min_delay` | PARTIAL | — | -fall_from, -fall_to, -from, -hold, -ignore_clock_latency, -rise_from, -rise_to, -setup, -through, -to |
| `set_min_pulse_width` | PARTIAL | — | -clock, -fall, -high, -low, -rise |
| `set_min_transition` | UNSUPPORTED | — | -clock, -fall, -max, -min, -rise |
| `set_multicycle_path` | PARTIAL | -fall_to, -hold, -rise_to, -setup | -end, -fall_from, -from, -rise_from, -start, -through, -to |
| `set_operating_conditions` | PARTIAL | -max | -analysis_type, -library, -min |
| `set_output_delay` | PARTIAL | -clock, -max, -min | -add_delay, -clock_fall, -disable_clock_gating_check, -fall, -level_sensitive, -network_latency_included, -reference_pin, -rise, -source_latency_included |
| `set_propagated_clock` | FULL | — | — |
| `set_sdc_version` | FULL | — | — |
| `set_timing_derate` | PARTIAL | -cell_delay, -early, -late, -net_delay | -clock_delay, -data_delay |
| `set_units` | FULL | -capacitance, -current, -power, -resistance, -time, -voltage | — |
| `set_voltage` | PARTIAL | — | -object_list |
| `set_wire_load_mode` | PARTIAL | — | -enclosed, -segmented, -top |
| `set_wire_load_model` | PARTIAL | — | -library, -max, -min, -name |

## Collection functions (NETLIST_REQUIRED by nature)

These resolve against the design; SDC-only analysis cannot prove them:

`add_to_collection`, `all_cells`, `all_clocks`, `all_inputs`, `all_nets`, `all_outputs`, `all_ports`, `all_registers`, `filter_collection`, `get_cells`, `get_clocks`, `get_lib_cells`, `get_nets`, `get_pins`, `get_ports`, `get_registers`, `remove_from_collection`

## Tcl constructs (TCL_EXECUTION_REQUIRED — never executed)

`array`, `break`, `catch`, `concat`, `continue`, `error`, `eval`, `exec`, `expr`, `for`, `foreach`, `format`, `global`, `if`, `lappend`, `lindex`, `list`, `llength`, `lrange`, `namespace`, `proc`, `puts`, `regexp`, `regsub`, `return`, `source`, `string`, `switch`, `uplevel`, `upvar`, `while`

## Notes

- **PARTIAL** means the command is recognized and its core semantics are
  checked, but some standard options are silently ignored. The analysis
  scope summary surfaces these per-run.
- **UNSUPPORTED** means the command parses as plain text with no analysis.
- Level is per-command; the per-run trust status is computed by
  `support_boundary.analyze_scope()` and attached to every `CheckResult`.
